import { describe, expect, it } from 'vitest'

describe('coverage glob fixture', () => {
  it('leaves the inventoried module unexecuted so include must enroll it', () => {
    expect(true).toBe(true)
  })
})
